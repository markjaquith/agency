---
"agency": minor
---

Add `--emit` flag as the primary option for specifying emit branch names in `emit`, `push`, `rebase`, and `task` commands. The `--branch` flag is retained as a deprecated alias for backward compatibility.
