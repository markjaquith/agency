---
"agency": minor
---

Add `--emit` flag as the primary option for specifying emit branch names in `emit`, `push`, and `rebase` commands. The `--branch` flag is retained as a deprecated alias for backward compatibility.
